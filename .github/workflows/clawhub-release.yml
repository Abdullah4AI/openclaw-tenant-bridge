name: ClawHub Release

on:
  workflow_dispatch:

jobs:
  publish:
    permissions:
      contents: read
      id-token: write
    uses: openclaw/clawhub/.github/workflows/package-publish.yml@4af2bd50a71465683dbf8aa269af764b9d39bdf5
    with:
      source: Abdullah4AI/openclaw-tenant-bridge
      ref: ${{ github.sha }}
      dry_run: false
      json: true
